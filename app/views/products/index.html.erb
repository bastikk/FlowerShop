<%= button_to "Create product", new_product_path, method: :get %>
<% @products.each do |product| %>
  <div class="table-of-contents">
    <h3><%= link_to product.title, product_path(product) %></h3>
    <p><%= product.description %></p>
  </div>
<% end %>